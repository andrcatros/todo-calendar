import React from "react";

import StyledDiv from "../styles/calendar-style";

const Calendar = (props) => {
  return (
    <div className="Calendar">
      <StyledDiv> this is placeholder text </StyledDiv>
    </div>
  );
};

export default Calendar;
