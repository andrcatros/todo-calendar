import React from "react";
import TaskList from "../components/task-list";

const Main = (props) => {
  return (
    <div className="Main">
      <TaskList />
    </div>
  );
};

export default Main;
